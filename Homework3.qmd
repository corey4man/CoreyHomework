# Homework 3
## 1
```{r}
# Setting variables
x <- 1.1
a <- 2.2
b <- 3.3

# 1a
z <- x^(a^b)
print(z)

# 1b
z <- (x^a)^b
print(z)

#1c
z <- (3*x^3) + (2*x^2) + 1
print(z)

# 2

# 2a
vec1 <- c(seq(from=1, to=8), seq(from = 7, to = 1))
print(vec1)

# 2b
vec2 <- c(1:5)
rep(x = vec2, times = vec2) # Assigned each number to repeat the same amount of times as its position in the vector

#2c
vec3 <- seq(from =5, to =1)
rep(x = vec3, times = vec2) #Have each number in sequence vec3 repeat the amount of times of its position in vec2

#3
set.seed(123)
nums <- runif(2)
r <- sqrt((nums[1]^2+nums[2]^2))
#Used formula for converting x to r by using the positions of the random numbers in the random uniform distribution
theta <- atan(nums[2]/nums[1])
#Used formula for converting y to theta by using the positions of the random numbers in the random uniform distribution

#4
queue <- c("sheep", "fox", "owl", "ant")
queue <- c(queue, "serpent")
queue <- queue[-1]
queue <- c("donkey", queue)
queue <- queue[-5]
queue <- queue[-3]
queue <- c(queue[1:2], "aphid", queue[3])
which(queue == "aphid")

#5
ints <- (1:100)
newints <- ints[which(ints%%2 !=0 & ints%%3 !=0 & ints%%7 !=0)]
# Included only numbers that have a nonzero remainder when divided by 2,3, or 7
```